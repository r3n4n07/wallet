import { View, Text } from 'react-native';
import { styled } from 'nativewind';
import { Icon} from 'react-native-paper';

export const Container = styled(View);
export const ViewAvatar = styled(View);
export const Avatar = styled(Icon);
export const ViewTransaction = styled(View);
export const TransactionName = styled(Text);
export const TransactionHour = styled(Text);
export const Cash = styled(Text);
